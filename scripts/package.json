
{
  "name": "database-backup-scripts",
  "version": "1.0.0",
  "description": "Database backup scripts for Supabase PostgreSQL",
  "scripts": {
    "backup": "node backup-database.js",
    "backup:cron": "node backup-database.js"
  },
  "dependencies": {
    "dotenv": "^16.0.0"
  },
  "engines": {
    "node": ">=14.0.0"
  }
}
